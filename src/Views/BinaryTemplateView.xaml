<UserControl x:Class="BinaryDataReaderApp.Views.BinaryTemplateView" 
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:local="clr-namespace:BinaryDataReaderApp" 
	xmlns:models="clr-namespace:BinaryDataReaderApp.Models" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">

	<StackPanel>
		<TreeView ItemsSource="{Binding BinaryTemplate.Parts}">
			<TreeView.Resources>
				<HierarchicalDataTemplate DataType="{x:Type models:BinarySection}" ItemsSource="{Binding Parts}">
					<TextBlock Text="{Binding Name}" />
				</HierarchicalDataTemplate>
				<DataTemplate DataType="{x:Type models:BinaryValue}">
					<TextBlock Text="{Binding Name}" />
				</DataTemplate>
			</TreeView.Resources>
		</TreeView>
	</StackPanel>
</UserControl>